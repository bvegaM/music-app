import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.music.uets',
  appName: 'music',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
